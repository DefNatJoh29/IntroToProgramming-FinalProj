// for debugging purposes

document.addEventListener("keydown", (e) => {
    console.log(e)
    console.log(`Key: "${e.key}" | Code: "${e.code}" Shift: "${e.shiftKey}"`)
})

console.log(themeBtn.value.trim())

